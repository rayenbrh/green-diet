{
  "name": "green-diet",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n api,web,adm -c blue,green,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\" \"npm run build:watch --prefix frontend/admin\""
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
