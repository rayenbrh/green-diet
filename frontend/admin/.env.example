# En dev, build servi par Express : utiliser /api (même origine que :5000)
VITE_API_URL=/api
# Pour un build testé via `vite preview` ou dev isolé sur un autre port :
# VITE_API_URL=http://localhost:5000/api
