# Admin : en prod le build est dans frontend/Dockerfile (étape admin-build).
# En local, HMR + proxy vers l’API :

VITE_API_URL=/api
# Origine de l’API (sans /api) pour les prévisualisations /uploads/
# VITE_BACKEND_ORIGIN=http://localhost:5001
