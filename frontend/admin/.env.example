# En dev, build servi par Express : utiliser /api (même origine que :5001)
VITE_API_URL=/api
# Pour un build testé via `vite preview` ou dev isolé sur un autre port :
# VITE_API_URL=http://localhost:5001/api
